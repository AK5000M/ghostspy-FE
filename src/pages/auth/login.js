import React from "react";
import { LoginContent } from "../../page-content/auth/login";

const Login = () => {
  return (
    <React.Fragment>
      <LoginContent />
    </React.Fragment>
  );
};

export default Login;
