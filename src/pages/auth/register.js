import React from "react";
import { RegisterContent } from "../../page-content/auth/register";

const Register = () => {
  return (
    <React.Fragment>
      <RegisterContent />
    </React.Fragment>
  );
};

export default Register;
